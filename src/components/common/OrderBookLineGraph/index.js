import React, { useState } from 'react';
import styled from 'styled-components';

const FlexWrapper = styled.div`

`;



const OrderBookLineChart = props => {


  return (
    <FlexWrapper>
      <h2>This should become the line chart..</h2>
    </FlexWrapper>        
  );
}

export default OrderBookLineChart;
